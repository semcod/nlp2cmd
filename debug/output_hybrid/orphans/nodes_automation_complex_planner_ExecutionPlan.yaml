module: automation.complex_planner.ExecutionPlan
type: orphan_nodes
count: 3
nodes:
  automation.complex_planner.ExecutionPlan.to_dict_entry:
    id: automation.complex_planner.ExecutionPlan.to_dict_entry
    type: ENTRY
    label: entry
    function: automation.complex_planner.ExecutionPlan.to_dict
  automation.complex_planner.ExecutionPlan.to_dict_exit:
    id: automation.complex_planner.ExecutionPlan.to_dict_exit
    type: EXIT
    label: exit
    function: automation.complex_planner.ExecutionPlan.to_dict
  automation.complex_planner.ExecutionPlan.to_dict_return_60:
    id: automation.complex_planner.ExecutionPlan.to_dict_return_60
    type: RETURN
    label: return
    function: automation.complex_planner.ExecutionPlan.to_dict
    line: 60
